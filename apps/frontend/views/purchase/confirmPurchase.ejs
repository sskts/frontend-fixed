<% layout('../purchase/layout') -%>
<% block('script', '<script src="/js/purchase/confirmPurchaser.js"></script>') -%>
<div class="confirm-purchaser">
    <h1 class="page-ttl">購入者内容確認</h1>
    <p class="read">
        ご希望の座席を選択して次へボタンを押してください。予約できる座席枚数は最大4席までとなります。<br>
        画面をタッチすると画面がズームしますので座席を選択してください。
    </p>

    <form method="post">
        <h2 class="section-ttl">作品情報</h2>
        <ul class="box">
            <li>
                <dl>
                    <dt>劇場 / スクリーン</dt>
                    <dd><%= (data) ? data.facilityName : '' %> / <%= (data) ? data.screenName : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>作品名</dt>
                    <dd><%= (film) ? film.titleOfWork : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>鑑賞日時</dt>
                    <dd><%= (data) ? data.screeningDate : '' %> <%= (data) ? data.screeningStartTime : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>座席 / 券種</dt>
                    <dd>
                        <ul>
                        <% for (let seat of seats) { %>
                        <li><%= seat.code %> <%= seat.type %></li> 
                        <% } %>
                        </ul>
                    </dd>
                </dl>
            </li>
        </ul>       
        
        <h2 class="section-ttl">予約情報</h2>
        <ul class="box">
            <li>
                <dl>
                    <dt>お名前（ひらがな）</dt>
                    <dd><%= (info) ? info.lastNameHira : '' %> <%= (info) ? info.firstNameHira : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>メールアドレス</dt>
                    <dd><%= (info) ? info.mail : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>電話番号</dt>
                    <dd><%= (info) ? info.tel : '' %></dd>
                </dl>
            </li>
        </ul>        
        
        <h2 class="section-ttl">支払情報</h2>
        <ul class="box">
            <li>
                <dl>
                    <dt>カード番号</dt>
                    <dd><%= (info) ? info.creditNumber : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>カード有効期限</dt>
                    <dd><%= (info) ? info.creditYear : '' %> / <%= (info) ? info.creditMonth : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>ご利用金額</dt>
                    <dd>￥5,100</dd>
                </dl>
            </li>
        </ul>
        
        
        

        <div class="button-area clearfix">
            <div class="button next-button blue-button"><button type="submit">購入完了</button></div>
            <div class="button prev-button white-button"><button onclick="history.back();">戻る</button></div>
        </div>
        <input type="hidden" name="provisionalReservationNumber" value="<%= provisionalReservationNumber %>">
    </form>
</div>