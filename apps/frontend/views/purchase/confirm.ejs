<% layout('../purchase/layout') -%>
<% block('script', '<script src="/js/purchase/confirm.js"></script>') -%>
<div class="confirm">
    <h1 class="page-ttl">購入者内容確認</h1>
    <p class="read">
        ご希望の座席を選択して次へボタンを押してください。予約できる座席枚数は最大4席までとなります。<br>
        画面をタッチすると画面がズームしますので座席を選択してください。
    </p>

    <form method="post">
        <h2 class="section-ttl">作品情報</h2>
        <ul class="box bg-light-gray">
            <li>
                <dl>
                    <dt>劇場 / スクリーン</dt>
                    <dd><%= (data) ? data.facilityName : '' %> / <%= (data) ? data.screenName : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>作品名</dt>
                    <dd><%= (film) ? film.titleOfWork : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>鑑賞日時</dt>
                    <dd><%= (data) ? data.screeningDate : '' %> <%= (data) ? data.screeningStartTime : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>座席 / 券種</dt>
                    <dd>
                        <ul>
                        <% for (let seat of seats) { %>
                        <li><%= seat.code %> <%= seat.type %></li> 
                        <% } %>
                        </ul>
                    </dd>
                </dl>
            </li>
        </ul>       
        
        <h2 class="section-ttl">予約情報</h2>
        <ul class="box bg-light-gray">
            <li>
                <dl>
                    <dt>お名前（ひらがな）</dt>
                    <dd><%= (info) ? info.lastNameHira : '' %> <%= (info) ? info.firstNameHira : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>メールアドレス</dt>
                    <dd><%= (info) ? info.mail : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>電話番号</dt>
                    <dd><%= (info) ? info.tel : '' %></dd>
                </dl>
            </li>
        </ul>        
        
        <h2 class="section-ttl">支払情報</h2>
        <ul class="box bg-light-gray">
            <li>
                <dl>
                    <dt>カード番号</dt>
                    <dd><%= (gmoTokenObject) ? gmoTokenObject.maskedCardNo : '' %></dd>
                </dl>
            </li>
            <li>
                <dl>
                    <dt>ご利用金額</dt>
                    <dd>￥5,100</dd>
                </dl>
            </li>
        </ul>
        <input type="hidden" name="token" value="<%= (gmoTokenObject) ? gmoTokenObject.token : '' %>">
        <input type="hidden" name="toBeExpiredAt" value="<%= (gmoTokenObject) ? gmoTokenObject.toBeExpiredAt : '' %>">
        <input type="hidden" name="isSecurityCodeSet" value="<%= (gmoTokenObject) ? gmoTokenObject.isSecurityCodeSet : '' %>">
        
        

        <div class="button-area clearfix">
            <div class="button next-button blue-button"><button type="submit">購入完了</button></div>
            <div class="prev-link"><a href="<%- url('purchase.enterPurchase') %>">戻る</a></div>
        </div>
        <input type="hidden" name="reservationNo" value="<%= reservationNo %>">
    </form>
</div>

<%- partial('complete') %>